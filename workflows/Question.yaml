kind: AdaptiveDialog
beginDialog:
  kind: OnActivity
  id: workflow_demo
  actions:

      # Respond with input
    - kind: SendActivity
      id: sendActivity_demo
      activity: "Working..."
      
      # Use AI to answer the question
    - kind: AnswerQuestionWithAI
      id: asst_orsBf06Bxz9B1hjVjiiQoPqf
      variable: Topic.Answer
      userInput: =System.LastMessage.Text
