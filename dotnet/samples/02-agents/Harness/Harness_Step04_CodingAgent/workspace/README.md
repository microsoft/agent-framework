# Sample workspace

This folder ships with `Harness_Step04_CodingAgent` and is copied next to the executable as
`workspace/` at build time. The agent is sandboxed to this folder.

Try asking the agent to:

- List, search, and read these files.
- Add a new file or function and a unit test for it.
- Rename or delete a file (you will be asked to approve).
