// ------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
// </auto-generated>
// ------------------------------------------------------------------------------

#nullable enable
#pragma warning disable IDE0005 // Extra using directive is ok.

using System;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Agents.Workflows;
using Microsoft.Agents.Workflows.Declarative;
using Microsoft.Agents.Workflows.Declarative.Kit;
using Microsoft.Extensions.AI;
using Microsoft.Extensions.AI.Agents;

namespace Test.Workflow;

public static class WorkflowProvider
{
    /// <summary>
    /// The root executor for a declarative workflow.
    /// </summary>
    internal sealed class RootMyWorkflowExecutor<TInput>(
        DeclarativeWorkflowOptions options,
        Func<TInput, ChatMessage>? inputTransform) :
        RootExecutor<TInput>("root_my_workflow", options, inputTransform)
        where TInput : notnull
    {
        protected override async ValueTask ExecuteAsync(TInput message, IWorkflowContext context, CancellationToken cancellationToken)
        {
            // Set environment variables

            // Set user variables to default values
            await context.QueueStateUpdateAsync("MyTable", UnassignedValue.Instance, "Topic").ConfigureAwait(false);
        }
    }

    internal sealed class SetVarExecutor(FormulaSession session) : ActionExecutor(id: "set_var", session)
    {
        protected override async ValueTask ExecuteAsync(IWorkflowContext context, CancellationToken cancellationToken)
        {
            object? value = await context.EvaluateExpressionAsync("[{id: 3}]").ConfigureAwait(false);

            await context.QueueStateUpdateAsync("MyTable", value, "Topic").ConfigureAwait(false);
        }
    }

    public static Workflow<TInput> CreateWorkflow<TInput>(
        DeclarativeWorkflowOptions options,
        Func<TInput, ChatMessage>? inputTransform = null)
        where TInput : notnull
    {
        // Create executor instances
        RootMyWorkflowExecutor<TInput> root = new(options, inputTransform);
        SetVarExecutor setVar = new(root.Session);

        // Define the workflow builder
        WorkflowBuilder builder = new(root);

        // Connect executors
        builder.AddEdge(root, setVar);

        // Build the workflow
        return builder.Build<TInput>();
    }
}
