// ------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
// </auto-generated>
// ------------------------------------------------------------------------------

#nullable enable
#pragma warning disable IDE0005 // Extra using directive is ok.

using System;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Agents.Workflows;
using Microsoft.Agents.Workflows.Declarative;
using Microsoft.Agents.Workflows.Declarative.Kit;
using Microsoft.Extensions.AI;
using Microsoft.Extensions.AI.Agents;

namespace Test.Workflow;

public static class WorkflowProvider
{
    /// <summary>
    /// The root executor for a declarative workflow.
    /// </summary>
    internal sealed class MyWorkflowRootExecutor<TInput>(
        DeclarativeWorkflowOptions options,
        Func<TInput, ChatMessage>? inputTransform) :
        RootExecutor<TInput>("my_workflow_Root", options, inputTransform)
        where TInput : notnull
    {
        protected override async ValueTask ExecuteAsync(TInput message, IWorkflowContext context, CancellationToken cancellationToken)
        {
            // Set environment variables

            // Set user variables to default values
            await context.QueueStateUpdateAsync("TestValue", UnassignedValue.Instance, "Topic").ConfigureAwait(false);
        }
    }

    internal sealed class SetInputExecutor(FormulaSession session) : ActionExecutor(id: "set_input", session)
    {
        protected override async ValueTask ExecuteAsync(IWorkflowContext context, CancellationToken cancellationToken)
        {
            object? value = await context.ReadStateAsync<object>("LastMessageText", "System").ConfigureAwait(false);

            await context.QueueStateUpdateAsync("TestValue", value, "Topic").ConfigureAwait(false);
        }
    }

    internal sealed class ActivityInputExecutor(FormulaSession session) : ActionExecutor(id: "activity_input", session)
    {
        protected override async ValueTask ExecuteAsync(IWorkflowContext context, CancellationToken cancellationToken)
        {
            string activityText =
                await context.FormatTemplateAsync(
                    """
                    Input: {Topic.TestValue}
                    """
                );
            AgentRunResponse response = new([new ChatMessage(ChatRole.Assistant, activityText)]);
            await context.AddEventAsync(new AgentRunResponseEvent(this.Id, response)).ConfigureAwait(false);
        }
    }

    public static Workflow<TInput> CreateWorkflow<TInput>(
        DeclarativeWorkflowOptions options,
        Func<TInput, ChatMessage>? inputTransform = null)
        where TInput : notnull
    {
        // Create executor instances
        MyWorkflowRootExecutor<TInput> root = new(options, inputTransform);
        SetInputExecutor setInput = new(root.Session);
        ActivityInputExecutor activityInput = new(root.Session);

        // Define the workflow builder
        WorkflowBuilder builder = new(root);

        // Connect executors
        builder.AddEdge(root, setInput);
        builder.AddEdge(root, activityInput);

        // Build the workflow
        return builder.Build<TInput>();
    }
}

