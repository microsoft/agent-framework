kind: Workflow
trigger:

  kind: OnConversationStart
  id: my_workflow
  actions:

    - kind: InvokeAzureAgent
      id: invoke_agent
      conversationId: =System.ConversationId
      agent:
        name: TestAgent
