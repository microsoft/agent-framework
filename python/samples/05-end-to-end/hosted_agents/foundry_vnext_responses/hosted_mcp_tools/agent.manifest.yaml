name: echo-agent-responses-streaming
description: >
  A simple echo agent that streams responses word-by-word using the
  azure-ai-agentserver-responses SDK with Server-Sent Events (SSE).
metadata:
  tags:
    - AI Agent Hosting
    - Azure AI AgentServer
    - Responses Protocol
    - Streaming
template:
  name: echo-agent-responses-streaming
  kind: hosted
  protocols:
    - protocol: responses
      version: v0.1.0